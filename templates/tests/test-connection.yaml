apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-frontend-app.fullname" . }}-test-connection"
  labels:
    {{- include "react-frontend-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-frontend-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
